PolygonBrush = Klass({
  // draw the brush path at both endpoints
  // for each path segment, draw a quad from
  // one endpoint to the other
});

RoundBrush = Klass({
  // draw using lineCap = round, lineJoin = round (..?)
});
